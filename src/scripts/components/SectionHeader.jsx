const SectionHeader = ({ sectionTitle }) => (
  <header className="section-header has-background">
    <h2 className="section-header__headline medium-margin-bottom">{sectionTitle}</h2>
  </header>
);

export default SectionHeader;
