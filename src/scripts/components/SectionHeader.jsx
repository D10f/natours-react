const SectionHeader = ({ sectionTitle }) => (
  <header className="section-header">
    <h2 className="section-header__headline has-background medium-margin-bottom">{sectionTitle}</h2>
  </header>
);

export default SectionHeader;
